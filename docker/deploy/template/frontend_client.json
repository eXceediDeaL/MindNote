{
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    },
    "AllowedHosts": "*",
    "server": {
        "Identity": "{BackendIdentity}",
        "Api": "{BackendApi}",
        "Client": "{FrontendClient}"
    },
    "identityClient": {
        "ClientId": "frontend.client",
        "ClientSecret": "{SecretFrontendClient}"
    }
}