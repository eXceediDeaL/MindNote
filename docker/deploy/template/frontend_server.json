{
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    },
    "AllowedHosts": "*",
    "server": {
        "Identity": "{BackendIdenty}",
        "Api": "{BackendApi}"
    },
    "identityClient": {
        "ClientId": "frontend.server",
        "ClientSecret": "{SecretFrontendServer}"
    }
}