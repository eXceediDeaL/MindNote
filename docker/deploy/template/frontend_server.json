{
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    },
    "AllowedHosts": "*",
    "server": {
        "Identity": "{ServerIdentity}",
        "Api": "{ServerApi}"
    },
    "identityClient": {
        "ClientId": "frontend.server",
        "ClientSecret": "{SecretServerHost}"
    }
}