{
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    },
    "AllowedHosts": "*",
    "server": {
        "Identity": "{ServerIdentity}",
        "Api": "{ServerApi}"
    },
    "identityClient": {
        "ClientId": "server.host",
        "ClientSecret": "{SecretServerHost}"
    }
}