{
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    },
    "AllowedHosts": "*",
    "server": {
        "Identity": "{ServerIdentity}",
        "Api": "{ServerApi}"
    },
    "identityClient": {
        "ClientId": "client.host",
        "ClientSecret": "{SecretClientHost}"
    }
}