@page
@model MindNote.Server.Host.Pages.Nodes.IndexModel
@{
    ViewData["Title"] = "Nodes";
    ViewData["Id"] = "Nodes";
}

<ul class="breadcrumb">
    <li class="breadcrumb-item"><a asp-page="/Index">MindNote</a></li>
    <li class="breadcrumb-item active">@ViewData["Title"]</li>
</ul>

<div class="text-center" style="width:100%">
    <form method="post" class="form-inline" style="display:inline-flex">
        <label class="control-label">Id</label>
        <input class="form-control" asp-for="PostData.QueryId" style="margin-left:10px; width:100px" />
        <label class="control-label" style="margin-left:10px">Name</label>
        <input class="form-control" asp-for="PostData.QueryName" style="margin-left:10px; width:150px" />
        <label class="control-label" style="margin-left:10px">Content</label>
        <input class="form-control" asp-for="PostData.QueryContent" style="margin-left:10px; width:150px" />
        <button type="submit" asp-page-handler="Query" class="btn btn-outline-dark" style="margin-left:10px">
            <i class="fa fa-search"></i>
        </button>
        <a asp-page="./Edit" class="btn btn-outline-success" style="margin-left:10px">
            <i class="fa fa-plus"></i>
        </a>
    </form>
</div>

<table class="table table-hover" style="margin-top:10px">
    <thead>
        <tr>
            <th style="width:6%">
                Id
            </th>
            <th>
                Name
            </th>
            <th>
                Content
            </th>
            <th style="width:10%">
                Operations
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Data)
        {
            <tr>
                <td>
                    @item.Id
                </td>
                <td>
                    <a asp-page="./View" asp-route-id="@item.Id">@item.Name</a>
                </td>
                <td>
                    @item.Content
                </td>
                <td>
                    <div class="row" style="margin-left:0px">
                        <form method="post" style="margin-left:0px">
                            <input type="hidden" asp-for="PostData.Data.Id" value="@item.Id" />
                            <div class="btn-group-sm">
                                <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-outline-primary">
                                    <i class="fa fa-edit"></i>
                                </a>
                                <button type="submit" asp-page-handler="Delete" class="btn btn-outline-danger">
                                    <i class="fa fa-trash"></i>
                                </button>
                            </div>
                        </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>