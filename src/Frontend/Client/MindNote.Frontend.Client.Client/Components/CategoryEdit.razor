@using MindNote.Data.Raws

<EditForm Model="@Model" OnValidSubmit="@OnSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="input-group mt-2">
        <button type="submit" class="btn btn-primary form-control">Submit</button>
    </div>

    <div class="input-group mt-4">
        <div class="input-group-prepend">
            <span class="input-group-text">Name</span>
        </div>
        <InputText class="form-control" @bind-Value="@Model.Name" />
    </div>

    <div class="input-group mt-2">
        <div class="input-group-prepend">
            <span class="input-group-text">Class</span>
        </div>
        <InputSelect class="form-control" @bind-Value="@Model.Class">
            @foreach (var c in Enum.GetValues(typeof(ItemClass)))
            {
                <option value="@c">@Enum.GetName(typeof(ItemClass), c)</option>
            }
        </InputSelect>
    </div>

    <div class="input-group mt-2">
        <div class="input-group-prepend">
            <span class="input-group-text">Color</span>
        </div>
        <InputText class="form-control" @bind-Value="@Model.Color" type="color" />
    </div>
</EditForm>

@code {
    [Parameter]
    private EventCallback OnSubmit { get; set; }

    [Parameter]
    private CategoryEditModel Model { get; set; }
}
